<div class="container" >
  <h2> Change your booking dates</h2>
  <%= simple_form_for [ @boat, @booking ] do |f| %>
    <%= f.input :arrive, as: :date, html5: true %>
    <%= f.input :leave, as: :date, html5: true %>
    <%= f.submit "Change it!" %>
  <% end %>
</div>
